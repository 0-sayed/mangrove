# Project Bootstrap Status

This file is kept as the stable handoff point between planning and feature work.

Flow:

```text
planning/ -> bootstrap.md -> planning/roadmap/tasks.md
```

Bootstrap is complete. The runnable React + Vite + Phaser + TypeScript project
shape, test setup, and source folders are established.

Start gameplay and first-playable work from `planning/roadmap/tasks.md`.
Bootstrap owns generic repo/app setup only; gameplay behavior belongs in roadmap
tasks and source modules.
